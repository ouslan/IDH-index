---
title: "API development"
format:
  html:
    code-fold: true
jupyter: python3
---

```{python}
import os
os.chdir("..")
```

```{python}
import polars as pl
import geopandas as gpd
import requests
import ibis

ibis.options.interactive = True
```

```{python}
df = pl.read_csv('data/raw/data_ppr_2022_raw.csv')
df = df.select(['AGEP', 'SCH', 'SCHL','PWGTP'])
print(df)
```

```{python}
param = "AGEP,SCH,SCHL,HINCP,PWGTP,PUMA"
base = "https://api.census.gov/data/"
flow = "/acs/acs1/pumspr"
year = "2021"
url = f'{base}{year}{flow}?get={param}'
r = requests.get(url).json()
df = pl.DataFrame(r)
names = df.select(pl.col("column_0")).transpose()
names = names.to_dicts().pop()
names = dict((k, v.lower()) for k,v in names.items())
df = df.drop("column_0").transpose()
df = df.rename(names).with_columns(year=pl.lit(year)).cast(pl.Int64)

df
```

```{python}
gdf = gpd.read_file("data/external/tl_2019_72_puma10.zip")
.count()
```

```{python}
gdf[gdf["STATEFIP"] == "72"].count()
```


```{python}
df.select(pl.col("puma")).unique()
```